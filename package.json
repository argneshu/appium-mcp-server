{
  "name": "appium-mcp-server",
  "version": "0.1.53",
  "description": "Claude-compatible MCP server for Appium (Android + iOS)",
  "bin": {
    "appium-mcp-server": "./bin/appium-mcp-server.js"
  },
  "scripts": {
    "start": "node ./bin/appium-mcp-server.js",
    "postinstall": "echo '📦 Installing Python dependencies...' && (python3 -m pip install -r requirements.txt || echo '⚠️ Python dependencies not installed. Run manually: pip install -r requirements.txt')"
  },
  "author": "argneshu",
  "license": "MIT"
}
